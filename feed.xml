---
layout: none
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ site.lang }}">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" {% if site.lang %}hreflang="{{ site.lang }}" {% endif %}/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ page.url | absolute_url | xml_escape }}</id>

  <title type="html">{{ site.title | smartify | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <author>
    <name>Octave community</name>
  </author>

  {%- assign pkgs = site.pages | where: "layout", "package" -%}
  {% assign i = 0 %}
  {%- for pkg in pkgs -%}
    {%- assign t = pkg.versions[0].date | date_to_xmlschema | append: "$" | append: i | append: ", " -%}
    {%- if t.size > 5 -%}
      {%- assign timesort = timesort | append: t -%}
    {%- endif -%}
    {% assign i = i | plus:1 %}
  {%- endfor -%}
  {%- assign timesort = timesort | split: ", " | sort | reverse | slice: 0, 10 -%}
  {%- for t in timesort -%}
    {%- assign tt = t | split: "$" | shift | join: ", " -%}
    {%- assign times = times | append: tt | append: ", " -%}
  {%- endfor -%}
  {%- assign times = times | split: ", " -%}

  {%- for t in times -%}
    {%- assign tt = t | to_integer -%}
    {%- assign pkg = pkgs[tt] -%}
    <entry xml:lang="{{ site.lang }}">
      <id>{{ pkg.permalink | absolute_url }}#{{ pkg.versions[0].id }}</id>
      <title type="html">{{ pkg.permalink }} {{ pkg.versions[0].id }} released</title>
      <link href="{{ pkg.permalink | absolute_url }}" rel="alternate" type="text/html" title="{{ pkg.permalink }}" />
      <published>{{ pkg.versions[0].date | date_to_xmlschema }}</published>
      <updated>{{ pkg.versions[0].date | date_to_xmlschema }}</updated>
      <summary type="html">
        <a  href="{{ pkg.icon | default: site.pkg_icon | absolute_url }}">
        <img src="{{ pkg.icon | default: site.pkg_icon | absolute_url }}" alt="{{ pkg.permalink }}" />
        </a>
        {{ pkg.description | strip_html | strip | escape }}
      </summary>
      <content type="html">
        <a  href="{{ pkg.icon | default: site.pkg_icon | absolute_url }}">
        <img src="{{ pkg.icon | default: site.pkg_icon | absolute_url }}" alt="{{ pkg.permalink }}" />
        </a>
        {{ pkg.description | strip_html | strip | escape }}
        Install {{ pkg.permalink }} {{ pkg.versions[0].id }} ({{ pkg.versions[0].date }})
        from the Octave command-line with:
        <pre><code>pkg install "{{ pkg.versions[0].url }}"</code></pre>
        {%- assign news = pkg.links | where: "label", "news" -%}
        {%- if news != empty -%}
        See what has changed in this release in the
        <a href="{{ news[0].url }}">NEWS</a> file.
        {%- endif -%}
      </content>
      <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ pkg.icon | default: site.pkg_icon | absolute_url }}"/>
      <media:content xmlns:media="http://search.yahoo.com/mrss/" medium="image" url="{{ pkg.icon | default: site.pkg_icon | absolute_url }}"/>
    </entry>
  {%- endfor -%}
</feed>
